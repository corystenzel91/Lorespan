# Lorespan v8.9.37

Movie-animation correction build.

- Uses the supplied 4-second movie for the visible book-opening sequence.
- Adds a movie-layer 3D shelf turn so the book leaves spine-first and rotates toward the desk.
- Includes a separately rendered reverse movie for returning the book to the shelf.
- Enlarges the final open-page view until it fills the screen.
- Keeps Skip Animation available in both directions.
- Falls back to the existing procedural animation if the movie cannot load.
- Updates the service-worker cache and performs HTML, JavaScript, asset, and archive checks.
