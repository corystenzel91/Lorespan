Delete every existing file in the GitHub Pages repository root. Extract this ZIP and upload every extracted file directly to the root so index.html is at the top level. Commit the upload, wait for Pages to deploy, then uninstall the old installed PWA once and reinstall it.
